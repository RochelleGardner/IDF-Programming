//  write a prgram in C to count the number of vowels and consonants in a string using a pointer

/*

int main func
    initialize a char string[256]
    char a *ptr
    init a place for the cons
    init a place for the vowelsele
    
    prompt a user for a string
    get that string into string
    assign ptr to string

    make the cons = the vowel = 0

    while *ptr is != nul char '\0'
        if *ptr == 'A' or 'a' or 'E' or 'e' or 'I' or 'i' or 'O' or 'o' or 'U' or 'u'
            increment the vowel
        else
            increment the cons
        
        increment ptr to get to the next char

    print "Total number of vowels is blank and total consonants is blank", vowlelselee, cons

    done

*/